<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.DittoRMApp" parent="android:Theme.Material.Light.NoActionBar" />
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <!-- Customize your app bar overlay here -->
        <item name="android:textColorPrimary">@color/abc_primary_text_material_dark</item>
        <item name="elevation">4dp</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <!-- Customize your popup overlay here -->
        <item name="android:textColor">@color/abc_secondary_text_material_light</item>
        <item name="android:background">@drawable/popup_background</item>
    </style>

    <style name="Theme.DittoRMApp." parent="Theme.MaterialComponents.Light.NoActionBar.Bridge"/>
</resources>